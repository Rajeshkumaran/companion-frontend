const getStyles = (theme) => ({
  appWrapper: {
    backgroundColor: theme.palette.brand0,
    height: '100%',
    overflowY:'auto',
    "::-webkit-scrollbar":{
      display:"none"
    },
  },
});
export default getStyles;
